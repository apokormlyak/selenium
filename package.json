{
  "name": "course_selenium",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha '${workspaceFolder}/test/**/*.js' --recursive --reporter mocha-junit-reporter --timeout 60000 --exit"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mocha": "^8.3.0",
    "selenium-webdriver": "^4.0.0-beta.1"
  }
}
